// ltl-segment-tree/main.ltl
//
// Segment tree with lazy propagation for range updates.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-segment-tree v0.1.0")
    io::println("Segment tree with lazy propagation for range updates.")
    0
}
